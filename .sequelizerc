const path = require("path");

module.exports = {
  config: path.resolve("src/config", "sequelize-cli.config.js"),

  migrationsPath: path.resolve("dist", "migrations"),
  seedersPath: path.resolve("dist", "seeders"),
  modelsPath: path.resolve("dist", "models"),
};
